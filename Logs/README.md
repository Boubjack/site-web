# Logs/

Runtime log output directory. The Logging subsystem's `FileSink` writes engine
logs here (e.g. `infinity.log`).

Log files are generated at runtime and **ignored by git**; only this README and
the `.gitignore` are tracked so the folder always exists.
