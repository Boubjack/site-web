# Specs/

Detailed technical specifications for systems that are large enough to warrant
a design document before implementation (e.g. ball physics model, player AI
decision pipeline, match state machine, save format versioning).

Specs are written **before** the corresponding gameplay code and are referenced
from the relevant module README and from `Docs/Roadmap.md`. None are required in
Mission 1 (foundation only).
