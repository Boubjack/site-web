# Source tree: the engine library, the football game library, and the
# executables built on top of them.
add_subdirectory(Engine)
add_subdirectory(Game)
add_subdirectory(Sandbox)
add_subdirectory(MatchSim)
