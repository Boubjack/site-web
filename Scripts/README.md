# Scripts/

Automation scripts for building, testing, formatting, and packaging the
project (shell / Python). Kept separate from `Tools/`, which hosts compiled
utilities.

No scripts are shipped in Mission 1 (foundation only). Suggested future
entries: `build.sh`, `test.sh`, `format.sh`, `package.sh`.
